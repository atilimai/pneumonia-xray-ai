num_epochs: 50
batch_size: 32
learning_rate: 0.001
scheduler_type: "CosineAnnealing"  
optimizer: "AdamW"                 
weight_decay: 0.0001
augmentation:
  enabled: true
  horizontal_flip: 0.5
  rotation_degrees: 15
checkpoint_save_freq: 5            
early_stopping_patience: 10        
